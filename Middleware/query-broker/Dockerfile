FROM golang:1.22-alpine AS build
WORKDIR /src
COPY go.mod ./
COPY *.go ./
RUN CGO_ENABLED=0 go build -ldflags="-s -w" -o /out/query-broker .

FROM gcr.io/distroless/static-debian12
COPY --from=build /out/query-broker /query-broker
EXPOSE 8081
ENTRYPOINT ["/query-broker"]
